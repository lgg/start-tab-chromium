name: Round 42 apply audited patch

on:
  pull_request:
    branches:
      - master
    types:
      - edited

permissions:
  contents: write

jobs:
  apply:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'audit/round42-full-project-20260801' &&
      contains(github.event.pull_request.body, 'APPLY_ROUND42_CI_ADJUSTMENT')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout trusted audit branch
        uses: actions/checkout@v4
        with:
          ref: audit/round42-full-project-20260801
          fetch-depth: 0
          persist-credentials: true

      - name: Apply final workflow-safe CI adjustment
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse 9d086cbfe5f96a1dc512eabce3ff5b3baf1fd984)" = "9d086cbfe5f96a1dc512eabce3ff5b3baf1fd984"
          python .round42/adjust.py
          rm -rf .round42
          git diff --check
          test ! -e .round42
          test -z "$(git diff --name-only | grep '^\.github/workflows/' || true)"

      - name: Commit final non-workflow adjustment
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A -- ':!.github/workflows'
          git commit -m 'Round 42: wire mandatory regression validation'
          git push origin HEAD:audit/round42-full-project-20260801
