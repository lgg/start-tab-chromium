name: Round 40 source export

on:
  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - .audit/round40-export-trigger
      - .github/workflows/round40-source-export.yml

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout exact audited master
        uses: actions/checkout@v6
        with:
          ref: b0df4fdcef634534c26cb84e63d5f806a4d7e85c
          fetch-depth: 1

      - name: Create tracked source archive
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "b0df4fdcef634534c26cb84e63d5f806a4d7e85c"
          git ls-files -z > "$RUNNER_TEMP/tracked-files.zlist"
          python - <<'PY'
          import hashlib
          import os
          import pathlib
          import tarfile

          root = pathlib.Path.cwd()
          names = [p.decode() for p in pathlib.Path(os.environ['RUNNER_TEMP'], 'tracked-files.zlist').read_bytes().split(b'\0') if p]
          archive = pathlib.Path(os.environ['RUNNER_TEMP'], 'start-tab-round40-source.tar.gz')
          with tarfile.open(archive, 'w:gz', format=tarfile.PAX_FORMAT) as tf:
              for name in names:
                  tf.add(root / name, arcname=name, recursive=False)
          digest = hashlib.sha256(archive.read_bytes()).hexdigest()
          pathlib.Path(os.environ['RUNNER_TEMP'], 'source-sha256.txt').write_text(f'{digest}  {archive.name}\n', encoding='utf-8')
          pathlib.Path(os.environ['RUNNER_TEMP'], 'source-metadata.txt').write_text(
              f'commit=b0df4fdcef634534c26cb84e63d5f806a4d7e85c\ntracked_files={len(names)}\nsha256={digest}\n',
              encoding='utf-8',
          )
          print(f'Exported {len(names)} tracked files: {digest}')
          PY

      - name: Upload exact source
        uses: actions/upload-artifact@v4
        with:
          name: round40-exact-source
          path: |
            ${{ runner.temp }}/start-tab-round40-source.tar.gz
            ${{ runner.temp }}/source-sha256.txt
            ${{ runner.temp }}/source-metadata.txt
          retention-days: 1
          if-no-files-found: error
